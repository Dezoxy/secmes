# No vulnerabilities are currently allow-listed.
#
# The transitive CVEs deferred during the Phase-0 scaffold were FIXED by the
# dependency refresh (NestJS 11 + vitest 4 + updated toolchain) — OSV now reports
# "no issues found". The temporary allow-list was therefore removed.
#
# Add a time-boxed entry here ONLY for a genuinely-deferred, assessed CVE:
#
#   [[IgnoredVulns]]
#   id = "GHSA-xxxx-xxxx-xxxx"
#   ignoreUntil = 2026-09-01T00:00:00Z
#   reason = "why it's safe to defer"
#
# Any new or unlisted vulnerability still fails CI.
