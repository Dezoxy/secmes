---
category: Primitives
---

Core reusable UI primitives from `apps/web/src/features/ui/` — the argus app's actual building
blocks (buttons, modals, avatars, state messaging, toasts). Dark surface, purple accent by
default (the app's runtime accent-switcher is not modeled here — every preview renders the
default theme).
