# argus production configuration — TEMPLATE. Copy the non-secret values into your deploy environment.
# SECRETS are never set here and never via env_file — see the SECRETS section at the bottom.
#
# This file documents what compose.prod.yaml expects. It contains NO real values — placeholders only.
# In production these are delivered by Slice 3: Azure Key Vault → systemd credential files / runtime values,
# fetched by the VM's Managed Identity (never committed, never long-lived in env at rest). See docs/deploy.md.

# ──────────────────────────────────────────────────────────────────────────────────────────────────────
# Non-secret config (invariant #5 permits these in the environment). Set on the host / in CD.
# ──────────────────────────────────────────────────────────────────────────────────────────────────────

# Container images produced + signed by cd.yml; CD sets these to the registry digest/tag.
ARGUS_API_IMAGE=ghcr.io/dezoxy/argus-api:latest
ARGUS_INGRESS_IMAGE=ghcr.io/dezoxy/argus-ingress:latest

# Backblaze B2 (S3-compatible, EU). The access-key-ID is NOT a secret (it rides in every presigned URL);
# the matching SECRET access key IS a secret → delivered as a file (see SECRETS below).
S3_ENDPOINT=https://s3.eu-central-003.backblazeb2.com
S3_REGION=eu-central-003
S3_BUCKET=argus-attachments
S3_ACCESS_KEY_ID=your-b2-key-id
# REDIS_URL: NOT used in PROD — redis requires AUTH, so deploy.sh generates a password-bearing
# `redis://:<redis_password>@redis:6379` into a tmpfs Docker-secret FILE and the api reads it via
# REDIS_URL_FILE (the password never lands in env / `docker inspect`). The line below is only the
# local/unauthenticated fallback default for the dev stack.
REDIS_URL=redis://redis:6379

# OIDC for the API (runtime, non-secret issuer + audience). The issuer is the self-hosted Zitadel in this
# stack (roadmap #9), reachable at https://auth.4rgus.com (Caddy host-splits that domain to Zitadel). The
# AUDIENCE is the Zitadel **project id** (NOT the SPA client id): Zitadel mints the access token's `aud` for
# the project, and the API's jwtVerify({audience}) must match it — the local provisioner emits
# `OIDC_AUDIENCE=$PROJECT_ID` for exactly this reason (the client id is only the SPA's `VITE_OIDC_CLIENT_ID`
# below). Both ids come from provisioning (G1). The API fetches JWKS from the issuer URL (a hairpin out
# through Cloudflare on first fetch, then cached).
OIDC_ISSUER=https://auth.4rgus.com
OIDC_AUDIENCE=your-zitadel-project-id

# Error tracking (#48) — OPTIONAL, DSN-GATED. Leave UNSET and server error tracking is a complete no-op (the
# default until arming). Set SENTRY_DSN to the self-hosted GlitchTip project DSN (or Sentry EU) to enable —
# it is wired into the api container (Slice A). Events are DEFAULT-DENY scrubbed before send: no message
# content, keys, tokens, Authorization headers, cookies, request bodies/query, or presigned URLs ever leave
# (invariant #2; see docs/threat-models/error-tracking.md). A Sentry DSN is a write-only INGEST key (can submit
# events, not read them), so env is acceptable (like the B2 key-id). SENTRY_RELEASE defaults to IMAGE_TAG.
# SENTRY_DSN=
# The mounted-file form (SENTRY_DSN_FILE → a `sentry_dsn` Docker secret) lands with the GlitchTip service in
# Slice B; the SDK already supports it, but compose doesn't mount the secret yet.

# OIDC for the PWA — BUILD-TIME (Vite embeds VITE_* into the static bundle). The Caddy image build reads
# these as build args; without VITE_OIDC_ISSUER + VITE_OIDC_CLIENT_ID the shipped client runs in demo mode
# (no real auth). Set them in CD when building the ingress image. NOT secrets.
VITE_OIDC_ISSUER=https://auth.4rgus.com
VITE_OIDC_CLIENT_ID=your-spa-client-id
VITE_OIDC_REDIRECT_URI=https://4rgus.com/auth/callback

# ──────────────────────────────────────────────────────────────────────────────────────────────────────
# SECRETS — never put values here. Delivered as FILES (invariant #5) or a runtime-fetched value:
#
#   secrets/postgres_password        → Postgres reads it via POSTGRES_PASSWORD_FILE
#   secrets/database_url             → the api reads it via DATABASE_URL_FILE. MUST use the non-bypass
#                                       RUNTIME role argus_app (NOT the argus owner), so RLS/grants bind even
#                                       on a missed SET-LOCAL-ROLE path or app compromise:
#                                         postgres://argus_app:<argus_app-pw>@postgres:5432/argus
#                                       Slice 3 grants argus_app LOGIN + a Key Vault password out-of-band
#                                       (see migration 0001). The argus owner credential stays SEPARATE,
#                                       used only for init + migrations (Slice 4: MIGRATION_DATABASE_URL).
#   secrets/s3_secret_access_key     → the api reads it via S3_SECRET_ACCESS_KEY_FILE (B2 key secret)
#   secrets/redis_password           → deploy.sh generates redis.conf (requirepass) + redis_url
#                                       (redis://:<pw>@redis:6379) from it; api reads REDIS_URL_FILE, redis's
#                                       healthcheck reads this file directly. No Redis creds in env. URL-safe
#                                       (e.g. openssl rand -hex 32).
#   secrets/zitadel_masterkey        → zitadel reads it via --masterkeyFile. 32 BYTES, generated ONCE, never
#                                       rotated casually (loss = the instance's encrypted data is unrecoverable)
#   secrets/zitadel_db_password      → zitadel-db reads it via POSTGRES_PASSWORD_FILE; zitadel reads the SAME
#                                       value as ${ZITADEL_DB_PASSWORD} (no Zitadel `_FILE` env form)
#   secrets/zitadel_login_pat        → zitadel-login reads it via ZITADEL_SERVICE_USER_TOKEN_FILE. OPTIONAL:
#                                       provisioned AFTER first init (Zitadel mints it then) + stored in Key
#                                       Vault during arming; the fetch seeds an empty file until then so the
#                                       mount resolves (login UI degraded until provisioned). NOT persisted to
#                                       a Docker volume (invariant #2). See docs/deploy.md "Zitadel bootstrap".
#   secrets/grafana_admin_password   → grafana reads it via GF_SECURITY_ADMIN_PASSWORD__FILE (observability
#                                       #47). Set it in Key Vault before the first deploy.
#
#   TUNNEL_TOKEN  (the Cloudflare tunnel token you added to env.local) — the ONE secret that can't be a
#     mounted file (cloudflared has no shell / no --token-file). Injected as a RUNTIME-FETCHED value into
#     the deploy environment (Slice 3: systemd sets it from Key Vault) — never an on-disk env file.
#   ZITADEL_DB_PASSWORD     — runtime value Zitadel reads (= the zitadel_db_password file; the TUNNEL_TOKEN
#     exception, since Zitadel has no `_FILE` env form for the DB password). deploy.sh sets it on `up`.
#   ZITADEL_ADMIN_PASSWORD  — FIRST-INIT bootstrap admin password (change-required), runtime value from
#     Key Vault. Read by FirstInstance on the very first init only; ignored on every later boot.
#
# The `secrets/` directory is gitignored. In production, Slice 3 generates these files / runtime values from
# Key Vault via Managed Identity at deploy time; they are NEVER committed nor baked into an image.
# ──────────────────────────────────────────────────────────────────────────────────────────────────────
