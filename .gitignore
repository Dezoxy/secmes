# dependencies
node_modules/
.pnpm-store/
.venv/

# Node V8 compile cache (NODE_COMPILE_CACHE) — regenerated per run, never committed
node-compile-cache/

# throwaway spikes — never product code
/spike/

# build output
dist/
build/
*.tsbuildinfo

# env / secrets — NEVER commit
.env
.env.*
!.env.example
!.env.prod.example
.netrc
.yarnrc.yml
*.pem
*.key
*.pfx
*.p12
*.jks
*.keystore
*.crt
*.cer
*.der
kubeconfig
*.kubeconfig
*kubeconfig*.yaml
id_rsa
id_dsa
id_ecdsa
id_ed25519
*_rsa
*_ed25519
*.gpg
*.asc
# Root-only runtime credential dir (compose local-dev secret files). Anchored with a leading slash so it does
# NOT match the tracked code dir infra/stack/secrets/ (the Key Vault fetch script + systemd unit).
/secrets/
.secrets/
*.secret

# terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
backend.hcl
!backend.hcl.example
crash.log
override.tf
override.tf.json

# logs / os
*.log
.DS_Store
coverage/
apps/web/playwright-report/
apps/web/test-results/
# browser-automation (Playwright-MCP) session snapshots/console logs at the repo root — never committed
.playwright-mcp/
.vscode/
.idea/

# claude code — personal per-project overrides (settings.json IS committed)
.claude/settings.local.json

# generated api spec (regenerated in CI)
apps/api/openapi.json

# load-test scratch artifacts (docs/operations/load-testing.md) — operator-run, never committed.
# loadtest-signing.pem is already covered by *.pem above; these are the rest.
loadtest-tokens.json
seed-loadtest.ts
messaging-load.js
