# dependencies
node_modules/
.pnpm-store/
.venv/

# throwaway spikes — never product code
/spike/

# build output
dist/
build/
*.tsbuildinfo

# env / secrets — NEVER commit
.env
.env.*
!.env.example
!.env.prod.example
.netrc
.yarnrc.yml
*.pem
*.key
*.pfx
*.p12
*.jks
*.keystore
*.crt
*.cer
*.der
kubeconfig
*.kubeconfig
*kubeconfig*.yaml
id_rsa
id_dsa
id_ecdsa
id_ed25519
*_rsa
*_ed25519
*.gpg
*.asc
# Root-only runtime credential dir (compose local-dev secret files). Anchored with a leading slash so it does
# NOT match the tracked code dir infra/vm/secrets/ (the Key Vault fetch script + systemd unit).
/secrets/
.secrets/
*.secret

# terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
override.tf
override.tf.json

# logs / os
*.log
.DS_Store
coverage/
apps/web/playwright-report/
apps/web/test-results/
# browser-automation (Playwright-MCP) session snapshots/console logs at the repo root — never committed
.playwright-mcp/
.vscode/
.idea/

# claude code — personal per-project overrides (settings.json IS committed)
.claude/settings.local.json

# generated api spec (regenerated in CI)
apps/api/openapi.json
