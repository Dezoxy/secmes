# argus — Build Roadmap: detailed history (archived)

> The **full annotated build log** — every checkpoint with its per-PR implementation notes, captured
> 2026-06-14. The live, slim checklist is [`roadmap.md`](roadmap.md); use this file for the detail behind
> any checkpoint. New status updates go in `roadmap.md`, not here.

---


Living checklist. Check items off as they land. Each checkpoint states its **done-when** so "complete" is objective. **Effort is per-item, not flat** — most are ~½–2 days, but a few (notably #41 core UX, #42, #43) are *weeks*; don't plan runway against an average. The implied "~10–12 weeks" is realistically **6–9 months solo**.

**Reality notes**

- Checkpoints **17–32 (crypto + messaging) are the hard, high-risk core** — most of the effort and all of the "is this actually secure" risk lives there. Don't rush them.
- Two GA gates (**G4 crypto review, G5 pen test**) are **external and paid** — schedule and budget them early; they block launch.
- This is a genuine multi-month solo effort. That's expected — the list just makes it honest.
- **Front-load the unknowns** (spikes S1–S2 below): the hardest thing (MLS) and the longest-lead-time thing (paid audits) start _now_, not in sequence.
- This roadmap is **canonical** for phasing; `secure_messaging_platform_plan.md` §17 is an earlier, looser cut — defer to this file when they disagree.
- Each phase is gated by its `docs/threat-models/*.md` note (rls-tenant-isolation, key-directory, key-backup, attachments) — ratify the note before the code.

Legend: `[ ]` todo · `[~]` in progress · `[x]` done · 🔒 security-gated (route through the matching reviewer).

**Status (2026-06-09):** building. **Phase 1 app layer DONE** — **Done:** 11–12 (Drizzle + RLS, PR #34), 14 + `GET /me` (JWT auth + tenant guard, PR #36), 16 (append-only audit log, PR #37), 15 (`/me` + JIT provisioning + `GET /users` directory, PR #38/#39). **13 done locally** — API JWT validation + the **SPA OIDC login** (oidc-client-ts PKCE → token → `/me`) now work end-to-end in the Docker stack; the VM's self-hosted Zitadel deploy rides #9/#10. **In progress:** S1 (`ts-mls` verified in Node; iOS-PWA proof pending — USER), 6 (CI green; image build/push + `az vm run-command` deploy ride the CD pipeline). **9 (Zitadel) now runs locally** — real OIDC login + end-to-end JWT validation in the Docker stack (this PR); the VM's self-hosted Zitadel stays the real checkpoint. **The Phase-0 VM deploy track is now code-complete + gated** — Slices 1–4 all merged (Terraform #94, prod ingress/Caddy/cloudflared #106, Key Vault→credential-files #110, release-on-tag CD #115); nothing is deployed yet (`vars.ENABLE_DEPLOY` off + the `prod` environment's required-reviewer gate), so **arming is a one-time manual Azure step**. App logic is still built/run locally first (Docker stack: `make up`). Checkpoint 10 (managed Postgres) is **superseded** — the VM self-hosts Postgres under RLS. **Phase 2 crypto IN PROGRESS:** 16a + 17 (MLS wrapper, PR #40) + 19 (server key directory, PR #41) done. 16a + 17 (MLS wrapper, PR #40), 19 (key directory, PR #41), 18 (keystore + build-order enabler, PR #42), 21 (`sealBackup`/`openBackup`, PR #43), 22 (server `key_backups` storage, PR #44), 18→sealed + 23 (sealed keystore at rest + fresh-device recovery, PR #45 — **18's unsealed gate lifted**), **20** (crypto review #1 — consolidated `key-model.md` + OpenAPI input bounds, PR #46; stays `[~]` until the Phase-5 **fingerprint-verification** MITM defense ships, which needs the client app #39), and **24** (CSPRNG audit — all randomness CSPRNG; `argus-no-insecure-random` total-ban rule, PR #47) done. **Phase 2 crypto primitives are now complete**; 20's fingerprint-verification MITM defense is now **BUILT** (safety-number crypto + Verify-security UI, this PR) — its only residual is applying it to a live remote peer (rides the message loop). **Phase 3 (1:1 encrypted text) server half DONE:** 25 (schema, #48), 26 (send API, #49), **26a (MLS Welcome delivery, this PR)**, 27 (fetch, #51), 28 (WS gateway, #52), 29 (Redis backplane, #53), 30 (catch-up sync, #54), 31 (delivery receipts, #55) + 32 (whole API documented + hardened to **42Crunch 100/100**, #56) — the client loop has since landed (41a, #66–#73), so **26a/27/30 are now `[x]`**; only **31** (live receipt push) stays `[~]`. **Phase 4 started:** 35's `attachments` table (METADATA + ciphertext-refs only, #57) + **33's server slice — presigned upload/download grants behind an S3-compatible `BlobStore` (A2)** — **migrated Azure Blob → Backblaze B2 (S3, EU `eu-central-003`) in PR-1, MinIO locally**; the client encrypt→upload→ref→download→decrypt wiring (34/36) is **DONE (A3, this PR)** — images send/receive end-to-end, server-blind. **Phase 5 STARTED — the client now exists:** 41 chat UI shell (conversation list / composer / delivery states / image chips, #58), **real in-browser MLS E2EE on send** (encrypt→decrypt round-trip via `@argus/crypto`, #59), 42 account-recovery UI (back up + data-loss-safe restore, #60), and the Vite PWA shell + manifest/SW (39). **The live client message loop is now COMPLETE** (41a, #66–#73): device provisioning → start-a-1:1 → join-on-connect → live send/fetch/WS-push → sealed message-history persistence, end-to-end E2EE over the real API; encrypted attachments send/receive end-to-end too (A3). **The 14-step `apps/web` maintainability + PWA/UX hardening pass is DONE (44a, #87–#146)** — canonical detail + standing rules stay in `docs/frontend-plan.md`. **Open fronts now:** (#9 self-hosted Zitadel in the prod Compose stack + #47 observability — Prometheus/Grafana/Alertmanager — are now **built as gated code**: #127, #133/#135; both await arming.) #42's server backup upload (`PUT`/`GET /backups/me` wiring + first-run prompt), #43 code-delivery hardening (CSP/SRI/SW-pinning), #40 Web Push, #44 a11y/responsive (WCAG AA audit sign-off; baseline in, #44a), #31's live receipt push, and the **one-time Azure arming** of the gated deploy track. Group chat / PCS handshake fan-out deferred (B1).

---

## Front-load — start now, parallel to Phase 0

- [~] S1. **MLS spike** (laptop, no cluster) — `ts-mls` two-party encrypt/decrypt + add-member, run RFC 9420 interop vectors, measure gzipped bundle size, **prove it on a real iOS-Safari installed PWA**, sketch an IndexedDB keystore. Ratifies `docs/mls-library-selection.md`. _Highest-leverage de-risking action — do it this week._ 🔒 — _Node portion VERIFIED (`ts-mls` 1.6.2 full 2-party flow), and the **in-browser** encrypt→decrypt round-trip is now demonstrated in-app + unit-tested (PR #59); the **IndexedDB keystore** landed (sealed at rest, PR #42/#45). Recorded with reproduction steps in `docs/mls-library-selection.md` § "Spike result" (the Node spike code is a gitignored throwaway, not committed). Remaining: interop vectors, gzipped bundle-size measurement, and the **iOS-Safari installed-PWA proof** — stays `[~]` until the iOS proof passes (USER)._
- [ ] S2. **Book the paid GA gates** — quotes + provisional calendar holds for G4 (crypto review) and G5 (pen test), ~2 months out. Lead time is the schedule risk, not the audits.

## Phase 0 — Platform foundation (VM + pipeline)

> Goal: stand up the VM and prove the deploy pipeline before the bulk of the app logic. (Kubernetes was dropped — the original AKS/GitOps Phase 0 was rewritten to the single-VM reality; recover the K8s checkpoints from git history if it is ever revisited.)

**Deploy-track status (2026-06-09): built as code + gated, NOT armed.** Slices 1–4 all merged — Slice 1 Terraform (#94), Slice 2 prod ingress topology (#106), Slice 3 Key Vault→credential files (#110), Slice 4 release-on-tag CD (#115). What remains is the **one-time Azure arming** (a real `terraform apply` + repo vars/secrets + the configured `prod` environment + a first tagged release), which is what flips #1/#3/#7/#8a — and the live half of the `[~]` items #2/#4/#5/#7a/#8 — to done. Threat models: `vm-ingress.md`, `vm-secrets.md`, `vm-cd.md`.

- [ ] 1. **VM provisioned** via Terraform (`infra/azure/`) — `terraform apply` clean; the Azure VM (EU `germanywestcentral`) boots with Docker + the Compose stack — _Terraform written (Slice 1, merged #94); flips to `[x]` on the one-time `terraform apply` (arming)._
- [~] 2. **Managed Identity → Key Vault** wired — the VM reads a Key Vault secret with no static creds (delivered as a credential file, not env) 🔒 — _Mechanism built as code (Slice 3, merged #110): `infra/stack/secrets/` — `argus-secrets.service` (hardened oneshot) runs `fetch-keyvault-secrets.sh`, which mints a Managed-Identity token via IMDS and writes each Key Vault secret to `/run/argus/secrets/*` (tmpfs, `0400` root, atomic, fail-closed, names-only logging). Threat model `vm-secrets.md`. **Not deployed** — install/enable is checkpoint 8/Slice 4._
- [ ] 3. **NSG deny-inbound proven** — the Azure NSG drops all inbound; the VM reaches out only via the Cloudflare Tunnel (no open ports) 🔒 — _The deny-inbound NSG is coded in the Terraform (#94); "proven" needs the live VM (arming)._
- [~] 4. **Ingress + TLS via Cloudflare** — cloudflared dials out, Cloudflare terminates TLS + runs the edge WAF/rate-limit; Caddy is a plain-HTTP single-origin reverse proxy (PWA + `/api` + `/ws`); admin subdomains behind Cloudflare Access — _Topology built as code (Slice 2, merged #106): `compose.prod.yaml` (standalone, no published ports, hardened) + the Caddy single-origin router (`infra/stack/caddy/`, image build-verified: non-root, serves PWA + SPA fallback + proxies `/api`,`/ws`) + the token-based cloudflared tunnel. Threat model `vm-ingress.md`. **Not deployed** — `vars.ENABLE_DEPLOY` off; CD rollout is checkpoint 5 (Slice 4). Cloudflare-side hostnames/Access stay dashboard-managed._
- [~] 5. **CD via `az vm run-command`** — GitHub Actions + Azure OIDC deploys the stack with no SSH and no open ports (works before the tunnel exists) — _Built as code (Slice 4, merged #115): **release-on-tag** (`v*.*.*`) — `cd.yml` builds/scans(Trivy)/signs(cosign) both version-tagged images → GHCR, then the `deploy` job bundles the exact-SHA infra config into an `az vm run-command` that runs `infra/stack/deploy/deploy.sh` (root, no SSH). Double-gated: `vars.ENABLE_DEPLOY` + the `prod` environment's required-reviewer approval. Threat model `vm-cd.md`. **Not armed** — needs the Azure sub + repo vars/secrets + the environment configured._
- [~] 6. **CI green on a PR** — lint/format/typecheck/test/build pass; GitHub→GHCR via OIDC — _CI green (ci · security · codeql); image build/scan/sign + **push to GHCR** + the run-command rollout now ride `cd.yml` (Slice 4, gated)._
- [ ] 7. **Hello-world `api` live** end-to-end over HTTPS through the Cloudflare Tunnel
- [~] 7a. **DB migrations run on deploy** — `az vm run-command` runs `db:migrate` (owner/migration credential from Key Vault, NOT the runtime `argus_app` role) **before** the new API container takes traffic, so a breaking migration (e.g. `0009` role rename) can never serve traffic ahead of its schema. — _Built (Slice 4, merged #115): `deploy.sh` brings up Postgres, then runs migrate as the owner via a file-mounted DSN (`MIGRATION_DATABASE_URL_FILE`, `shred`-ed after) before `compose up` of the api; migrations now ship in the image (nest `assets`). Lands when deploy is armed._ 🔒
- [~] 8. **Secrets via Key Vault** fetched by the VM's Managed Identity and mounted as credential files for the API container (never env at rest) 🔒 — _Delivery built (Slice 3, merged #110): the api reads `DATABASE_URL_FILE` (argus_app DSN) + `S3_SECRET_ACCESS_KEY_FILE`, postgres reads `POSTGRES_PASSWORD_FILE`, all sourced from `${ARGUS_SECRETS_DIR}=/run/argus/secrets` which `argus-secrets.service` fills from Key Vault; cloudflared `TUNNEL_TOKEN` is the one runtime-fetched value. Remaining: the Slice-4 deploy installs/enables the unit (templating the vault name) + runs `compose up` with the dir + token wired._
- [ ] 8a. **Staging + prod environments** stood up (per-env Compose config / subdomains, first deploy, `vars.STAGING_URL` registered) — the prod gate and nightly DAST both require this, and no other checkpoint creates it.

## Phase 1 — Identity & tenancy

> Goal: real login, real tenant isolation enforced by the database.

- [~] 9. **Zitadel deployed** (Docker Compose on the VM) with its DB — admin console reachable — _Local stand-in DONE (this PR): Zitadel v4 + its own Postgres in the Docker stack, bootstrapped reproducibly by `make up` (org + verified admin + project + SPA app + the `tenant_id`-claim Action) via `infra/local/zitadel/provision.sh`; console at `http://zitadel:8080`. Issues **JWT** access tokens carrying the verified UUID `tenant_id` (+ email/name), proven end-to-end with a real Authorization-Code+PKCE flow. The VM's self-hosted Zitadel (same Compose stack, secrets from Key Vault) stays the real checkpoint (Phase 0/infra). Runbook: `docs/local-auth.md`. **VM prod stack now BUILT (#127):** `compose.prod.yaml` runs self-hosted `zitadel` + its own Postgres + Login V2, hardened + no published ports; masterkey / DB-password / Login-V2 PAT from Key Vault (PAT never a persisted volume); Caddy host-splits `https://auth.4rgus.com` (Login V2 + `/api`-strip + h2c + TE-strip); Login V2 + OIDC default URLs wired at first boot. Threat model `vm-zitadel.md`. **Stays `[~]`** — awaiting the deploy (arming) + the one-time project/app/tenant-claim-Action provisioning (G1)._
- [x] 10. ~~**Managed Postgres** (Flexible Server) + private endpoint — reachable only in-VNet~~ 🔒 — **SUPERSEDED / N/A.** Deploy self-hosts Postgres on the VM via Docker Compose (AGENTS.md), so there is no Azure Flexible Server / private endpoint. The tenant-isolation guarantee this checkpoint protected is met by FORCE-RLS on the self-hosted DB (#11/#12). Recover from git history if managed PG is ever revisited (it becomes the enterprise upgrade for HA/PITR).
- [x] 11. **Drizzle wired** with a per-transaction `app.tenant_id` session var — _`withTenant()` (PR #34); pool `prepare:false` for PgBouncer txn mode._
- [x] 12. **`tenants` + `users` with RLS** — cross-tenant read provably blocked by a test 🔒 — _PR #34; non-bypass `argus_app` role, FORCE RLS + WITH CHECK, 8-test spec incl. pooled-reuse + privilege-escalation negatives._
- [x] 13. **OIDC login** via Zitadel works; API validates JWTs — _Both halves working end-to-end locally. API JWT validation (jose/JWKS: iss + aud + asymmetric-alg allowlist + exp/nbf; PR #36). **SPA login wired** (this PR): `oidc-client-ts` Authorization-Code+PKCE (public client; access token in an in-memory store per `auth-tenant-context.md` §8), `Callback` code→token exchange, an auth context + `RequireAuth` gate, and a Bearer-attached API client (`POST /auth/session` + `GET /me`). Proven: real login → JWT (verified `tenant_id` UUID + email/name from a Zitadel Action) → API validates via JWKS → JIT-provisions the user into the seeded tenant under RLS → `GET /me` 200; no/bad token → 401. The browser config (authority/client/redirect/PKCE) is verified against the running Zitadel. The VM's self-hosted Zitadel deploy rides #9/#10._
- [x] 14. **Tenant guard** sets `app.tenant_id` from the verified token only (never client input) 🔒 — _PR #36; global deny-by-default guard → `withTenant(verifiedTenantId)`; threat model `auth-tenant-context.md`._
- [x] 15. **`/me` + user directory** (per tenant) — Zod-validated, documented in the spec — _`GET /me` + JIT provisioning + `GET /users` directory DONE (Zod-validated via a reusable `ZodValidationPipe`, RLS-scoped, active-only, bounded `limit`; PR #36, #38, #39). Threat models `auth-tenant-context.md` §7 + `user-directory.md`._
- [x] 16. **Audit events** table + login/logout auditing (IDs/metadata only, no secrets) 🔒 — _PR #37; append-only `audit_events` (RLS; `argus_app` INSERT+SELECT only → tamper-resistant), `auth.login`/`auth.logout` via `POST`/`DELETE /auth/session`; 90-day retention policy (per-tenant worker prune later); threat model `audit-logging.md`._

## Phase 2 — Device keys & recovery (crypto foundation)

> Goal: the hard part. E2EE keys generated, published, and recoverable.

- [x] 16a. **Headless 2-device test harness** — a CLI/Node oracle doing encrypt→send→fetch→decrypt across two simulated devices, so checkpoints 17–38 (all _client_ behavior, but no client exists until #39) have a repeatable pass/fail instead of hand-verification. 🔒 — _PR #40; mock-server harness + a **server-blind assertion** (plaintext never appears in the wire bytes)._
- [x] 17. **MLS integrated** in `packages/crypto` — local encrypt/decrypt smoke test passes 🔒 — _PR #40; thin typed wrapper over `ts-mls` 1.6.2 (`MlsEngine`/`Conversation`); pinned suite, downgrade-resistant KeyPackage; crypto-reviewer PASS. **2-party scope** — group/PCS handshake fan-out deferred (B1); see `mls-integration.md`._
- [x] 18. **Device keys** generated client-side, stored in IndexedDB — _PR #42; `apps/web` `DeviceKeystore` (idb) generates via `@argus/crypto` + persists (race-safe, identity-checked). Also landed the monorepo **build-order enabler** (root `prepare` builds `packages/*` so `apps/web` can consume `@argus/crypto`). Originally **unsealed at rest** (dev/beta gate); **now SEALED at rest** — gate lifted in PR #45 (Argon2id + AES-256-GCM; IndexedDB schema v2 drops legacy unsealed records). Threat model `device-keystore.md`._
- [x] 19. **Key directory** — `devices` + `key_packages` tables (RLS); publish/fetch public KeyPackages 🔒 — _PR #41; `POST /devices/me/key-packages` (caller-bound device + pool cap) + `POST /users/:id/key-package/claim` (one-time-use, atomic FOR-UPDATE-SKIP-LOCKED, audited). Server stores PUBLIC base64 only. MITM defense = client-side fingerprint verification (**BUILT, #20** — `safetyNumber` + Verify-security UI). Threat model `key-directory.md`._
- [x] 20. **Crypto review #1** — crypto-reviewer pass + threat-model note for the key model 🔒 — _Review done: consolidated `docs/threat-models/key-model.md` over the whole key lifecycle (17–23); **`crypto-reviewer` PASS** (client crypto) + **`security-boundary-auditor` PASS** (server surface). Fixed the one Must-fix (OpenAPI input bounds + `additionalProperties:false` now mirror the enforced Zod) and hardened both key schemas with `.strict()`. **Fingerprint verification now BUILT + WIRED TO LIVE PEERS** (this PR): `@argus/crypto` `safetyNumber` (SHA-256 over both devices' identity keys — symmetric, deterministic, domain-separated, **stable across recovery re-mints**, identity-string-bound) + a **Verify-security** UI (out-of-band safety-number comparison, mark-verified that resets if the key changes, header badge) + threat model `fingerprint-verification.md`; **`crypto-reviewer` PASS** (no Must-fix). The two-phase safety-number-gated `ConversationManager` (`prepare()` + out-of-band confirm before `addMember`) applies to live remote peers via the completed message loop (41a). MITM defense fully operational._
> **⚠️ Checkpoints 21–23 + 42 were SUPERSEDED & REMOVED (2026-06, migration `0040_drop_key_backups.sql`).** The passphrase / Argon2id / server-stored `key_backups` backup-and-recovery scheme below — and the deleted `key-backup.md` threat model it cites — were replaced by the per-passkey **WebAuthn-PRF** keystore with **no server backup and no recovery** (a lost passkey is a fresh start). The entries are kept as historical record; the authoritative current model is `prf-keystore-unlock.md` / `key-model.md`.

- [x] 21. **Passphrase backup** — Argon2id-derived key encrypts private material client-side 🔒 — _PR #43; `@argus/crypto` `sealBackup`/`openBackup` (Argon2id 64 MiB/t3/p1 + AES-256-GCM, unique salt/IV, min-param floor, derived-key wipe). Generic over bytes; crypto-reviewer PASS. Threat model `key-backup.md`._
- [x] 22. **Backup storage** — `key_backups` table (ciphertext only) + backup/restore API 🔒 — _PR #44; `PUT`/`GET /backups/me` store/restore the opaque sealed blob (one per user, RLS, never parsed server-side), store+fetch audited. Threat model `key-backup.md`._
- [x] 23. **Recovery proven** — fresh browser → passphrase → restore → _the recovered **identity** works for MLS (PR #45). Per the identity-only / forward-secrecy decision (`key-backup.md` §4), pre-existing message **history is intentionally NOT recoverable** — this amends the original "decrypt an old message" wording. Also **lifts checkpoint 18's unsealed-at-rest gate** (keystore now sealed)._
- [x] 24. **CSPRNG audit** — no `Math.random` in security paths; Semgrep rule green 🔒 — _Audited every randomness source (`docs/threat-models/csprng-audit.md`): all key/nonce/salt/IV/ID/token material is CSPRNG (WebCrypto `getRandomValues`/`randomUUID`, `@noble` via ts-mls, pgcrypto `gen_random_uuid()`); zero `Math.random`/`pseudoRandomBytes`. Hardened `argus-no-insecure-random` (catches aliasing + `pseudoRandomBytes`; **total ban, no test exclusion**); enforced by `sast-semgrep` (`--error`)._

## Phase 3 — 1:1 encrypted text

> Goal: send and receive encrypted messages in real time.

- [x] 25. **Schema** — `conversations`, `conversation_members`, `messages` (RLS, ciphertext only) 🔒 — _PR #48; migration `0007_messaging.sql` — three tenant-scoped tables, all ENABLE+FORCE RLS + WITH CHECK + leading-`tenant_id` indexes; `messages` is **ciphertext-only** (opaque base64 + routing metadata, no plaintext column) and **append-only** (select/insert grant). Composite-FK tenant pinning beneath RLS; idempotency unique index. 9-test live-DB RLS spec (cross-tenant isolation, WITH CHECK, append-only, composite-FK, fail-closed) green. `security-boundary-auditor` PASS. Threat model `messaging-schema.md`. Intra-tenant membership authz is the app layer's job (26)._
- [x] 26. **Send API** — membership authz + Zod I/O + store ciphertext (no plaintext server-side) 🔒 — _PR #49; `messaging` module — `POST /conversations` (creator + members; cross-tenant member rejected by composite FK) + `POST /conversations/:id/messages` (CIPHERTEXT-ONLY store). **Membership authz** (the intra-tenant guard 25 deferred): non-member / cross-tenant / missing conversation → 404 (no existence leak); `sender_user_id` is the verified caller, never client-supplied. `.strict()` Zod + OpenAPI bounds + `additionalProperties:false`; conversation-scoped idempotency (0008). Live-DB specs (authz, cross-tenant, opaque round-trip, per-conversation idempotency) + full suite 86/86. `security-boundary-auditor` PASS._
- [x] 26a. **MLS Welcome delivery** — relay opaque join material so an added member can join the group 🔒 — _Server half DONE (this PR): migration `0012_welcomes.sql` — `conversation_welcomes` (tenant-scoped, ENABLE+FORCE RLS + WITH CHECK + composite-FK tenant pinning `(tenant_id, conversation_id)→conversations` CASCADE + leading-`tenant_id` indexes), **ciphertext-only** (`welcome`/`ratchet_tree` opaque base64 the server never decrypts) and **transient** (select/insert/delete — consumed on join, no UPDATE). A welcome is **bound to a specific recipient DEVICE** — the MLS Welcome is HPKE-sealed to one claimed KeyPackage, so the row carries `recipient_device_id` (composite FK `(tenant_id, recipient_user_id, recipient_device_id) → devices`), and list/consume are device-scoped so a second device of the same user can't fetch — or **consume/destroy** — a welcome it can't decrypt (Codex P2, fixed); and a welcome is **owned by the recipient's membership** (composite FK `(tenant_id, conversation_id, recipient_user_id) → conversation_members` CASCADE), so revoking app-level membership drops the stale join material before a removed member could fetch it (Codex P2 #2, fixed). `messaging` module: `POST /conversations/:id/welcomes` (deliver = invite; membership-gated, adds the member + stores the welcome atomically; recipient + device pinned to the tenant by composite FKs; `sender_user_id` = verified caller), `GET /welcomes?deviceId=&limit=` (the calling device's pending list — **metadata only**, never the blobs; **bounded `limit`** ≤100 so a member spamming an offline device can't make the connect-time fetch unbounded — Codex P2 #4, fixed), `GET /welcomes/:id/material?deviceId=&proof=` (the sealed join blobs — requires a **fetch** proof-of-possession, so a sibling session that spoofs a deviceId gets only ids from the list, never another device's join material — Codex P2 #5, fixed), `DELETE /welcomes/:id?deviceId=&proof=` (recipient-device-only consume — requires an Ed25519 **proof-of-possession** of the device's signature key over (deviceId, welcomeId), verified against the published public key, so a sibling session can't forge another device's consume; Codex P2 #3, fixed). The proof + verify live in a ts-mls-free `@argus/crypto/device-proof` module so the crypto-blind API verifies without loading the MLS WASM. `.strict()` Zod + OpenAPI bounds (32 KiB blobs — sized for the v1 1:1 add, kept under the ~100 KB JSON body cap) + `additionalProperties:false`; whole spec re-audited **42Crunch 100/100**. Live-DB + service tests (cross-tenant RLS, non-member 404, recipient-device-scoped fetch, recipient-device-only consume, wrong-device + multi-device 404, revoked-member cascade, proof-of-possession consume (sibling-device forgery + invalid-proof rejected), bounded-fetch limit, opaque round-trip, conversation-delete cascade, WITH CHECK, idempotent member-add). `security-boundary-auditor` PASS. Threat model `welcome-delivery.md`. **Client loop now COMPLETE** (41a, #66–#73): publish KeyPackage → claim peer → safety-number-gated `addMember` → deliver → join-on-connect → live send/fetch — all wired to the real API._
- [x] 27. **End-to-end text** — client MLS-encrypts → stored → recipient fetches → decrypts — _Server half DONE (PR #51): `GET /conversations/:id/messages` — member-only, keyset-paginated (cursor on `(created_at,id)`), returns opaque ciphertext + routing metadata verbatim (crypto-blind); same membership-404 as send; `requireMembership` factored out and shared. Live-DB tests (chronological order, pagination walk, non-member/cross-tenant 404, empty). `security-boundary-auditor` PASS. **Client loop now COMPLETE** (Slice 5B/5C, #71/#72): client MLS-encrypt → store → fetch-on-open + live WS push → decrypt, end-to-end over the real API; persisted to the sealed message-history log so it survives reload._
- [x] 28. **WebSocket gateway** — authenticated connections; real-time ciphertext delivery 🔒 — _PR #52; native `ws` gateway (`@nestjs/platform-ws`, no socket.io) at `/ws`. **First-frame token auth** (never in the handshake URL/headers), auth deadline closes silent sockets; **subscribe is membership-gated** (`isMember` under RLS); **delivery keyed by (tenant, conversation)** so fan-out never crosses a tenant or reaches a non-member; opaque ciphertext forwarded verbatim (crypto-blind). HTTP send emits on an in-process `RealtimeBus` (no module cycle, no extra dep). Global JWT guard tightened to skip only `ws`. Tests: 8 gateway unit (auth/authz/scoped-delivery/deadline/dead-socket-resilience) + 2 **real-WebSocket e2e** (adapter wiring). `security-boundary-auditor` PASS. Threat model `realtime-delivery.md`. **Single container** — the Redis backplane (29) is the realtime bus._
- [x] 29. **Redis backplane** — the realtime bus (and future throttler store) 🔒 — _PR #53; `RealtimeBus` is now abstract — `InProcessRealtimeBus` (single container / dev) or **`RedisRealtimeBus`** (when `REDIS_URL` set) which PUBLISHES each event and every subscriber SUBSCRIBES + fans out locally. Only the opaque ciphertext envelope crosses Redis (crypto-blind); incoming events Zod-validated (malformed dropped, no crash). **Backplane fan-out proven against live Redis** (two bus instances) + malformed-payload test. The deploy is a single API container on the VM, so the in-process bus suffices and the Redis backplane stays as the realtime bus (and the future throttler store) — `REDIS_URL` delivered as a Key Vault credential file (never plaintext). `security-boundary-auditor` + `infra-reviewer` PASS (their Must-fixes folded in). (The HPA / ≥2-pod cross-instance fan-out from the original AKS plan was dropped with Kubernetes.)_
- [x] 30. **Offline delivery** — queue + catch-up on reconnect — _Server half DONE (PR #54): the durable `messages` table is the offline queue; **`GET /sync`** returns messages across ALL the caller's conversations after a cursor (keyset, each tagged with `conversationId`) so a reconnecting client back-fills everything it missed in one paginated stream. Member-scoped (inner join under RLS); cursor resolved through the caller's memberships too (closes an intra-tenant existence/timing oracle — `security-boundary-auditor` Should-improve, fixed + tested). Live-DB tests: cross-conversation catch-up, non-member exclusion, pagination, cross-tenant isolation, cursor-oracle closed. `security-boundary-auditor` PASS. **Client catch-up on reconnect DONE** (Slice 5C, #72): on each reconnect the client re-auths + re-subscribes, and runs a **per-conversation** back-fill on each `subscribed` ACK (reuses 5B's `backfillConversation` from that conversation's own keyset cursor; dedup by server message id). Note: the cross-conversation **`GET /sync`** endpoint above exists server-side but is **not** wired client-side — the client catches up per conversation instead (a new conversation arrives via its Welcome/join, then back-fills on subscribe). The combined `/sync` client path stays a follow-up._
- [x] 31. **Delivery receipts** — sent/delivered/read end-to-end 🔒 — _Server half DONE (PR #55): `conversation_receipts` (0010, tenant-scoped + FORCE RLS + composite-FK tenant pinning) stores per-member delivered/read **high-water-marks** (metadata only). `POST /conversations/:id/receipts` advances the caller's own watermark (member-only, monotonic — no rollback; `throughMessageId` must be in the conversation); `GET …/receipts` returns per-member watermarks. Live-DB tests (record, monotonic, member-only 404, cross-tenant, foreign-message). `security-boundary-auditor` PASS (the `sql.raw(status)` splice is enum-gated; no injection). **Live push + client wiring now DONE:** `recordReceipt` emits a `ReceiptAdvancedEvent` on the realtime bus (in-process + Redis channel `argus:realtime:receipt-advanced`, Zod-validated) → the gateway fans a `receipt` frame to the conversation room (same room-scoped authz as messages, metadata only, internal user id) → the sender's client folds the peer's watermark onto its own messages so ticks flip live (single ✓ → ✓✓ delivered → "read"), replacing the demo timeouts for **live** conversations (demo/seed convs keep timeout ticks). The receiver POSTs `delivered` for every live conversation on receive and `read` for the open+focused conversation; **read sending is opt-in** (`features/settings/PrivacySettings`, storage extracted to `privacy-settings.ts`) with a **reciprocal display cap** (toggle off ⇒ don't send `read` AND don't render peers' `read`). Pure logic (`features/chat/receipts.ts`: watermark fold + post-decision) is unit-tested; threat-model `delivery-receipts.md` updated. Phase-3 client residual closed._
- [x] 32. **API security** — messaging endpoints in OpenAPI; 42Crunch audit ≥ 75 🔒 — _PR #56; the whole API surface (messaging + identity + key directory + backups) is documented and hardened to a **42Crunch audit score of 100/100** (up from 20.5). Centralized in `apps/api/src/openapi.ts`: a single HTTPS server + global bearer (`bearerFormat: JWT`) clears the transport / "token-in-clear" criticals; a shared `ErrorResponse` envelope + injected standard responses (400/401/403/404/406/415/default — **429 added once rate limiting shipped, #46**) give every error a typed body; a recursive tightener pins `additionalProperties:false` + a real `pattern`/`maxLength`/`maximum`/`maxItems` on every schema and parameter (uuid / date-time / email / base64 / base64url / tag / bounded-text — never a catch-all). Infra probes (`/healthz`, `/`) excluded from the audited contract. Audited in Free-Trial CLI mode; `ENABLE_42CRUNCH` stays false in CI._

## Phase 4 — Encrypted images

> Goal: encrypted attachments, blobs the server can't read.

- [x] 33. **Presigned upload** — private bucket + presigned grant API 🔒 — _Server half DONE (A2, this PR): a `BlobStore` abstraction (mirrors `RealtimeBus`) — **`S3BlobStore`** (AWS SDK v3, SigV4 query-presign): **MinIO** locally / **Backblaze B2** (EU `eu-central-003`) in prod with a **bucket-scoped app key from Key Vault via the VM's Managed Identity**, selected by a `useFactory` when `S3_*` config is present, else a fail-closed `UnconfiguredBlobStore` _(A2 shipped on Azure Blob+SAS; migrated to B2 in PR-1)_. `messaging` module: `POST /attachments` (mint upload grant + write the metadata row — **member-only**, `byteSize` Zod-capped at **10 MiB**, `object_key` server-minted + tenant-prefixed, `uploaded_by` from the **verified token only**) and `POST /attachments/download-url` (member-only download grant; authz from the **row's** `conversation_id`, never a client message ref — no IDOR; opaque `objectKey` body, never a URL; **hard-enforces the 10 MiB cap on the blob's ACTUAL size** — 413, since an S3 presigned PUT can't bind Content-Length). Presigns run **outside** the RLS tx; **300 s** TTL, single object + verb; the presigned URL is **never logged or persisted** (invariant #2). Membership authz (`requireUser`/`requireMembership`, same-404 no existence oracle) extracted to a shared `messaging/membership.ts` — one impl across the attachment + message paths (no IDOR drift). Live-DB membership tests (member upload writes a tenant-prefixed row; non-member / other-tenant / unknown-key all 404) + schema-bounds tests + OpenAPI (`POST /attachments` + `/attachments/download-url`, bearer auth, bounded). `security-boundary-auditor` + `infra-reviewer` PASS. Threat model `encrypted-attachments.md`. **Stays `[~]`:** the client wiring — encrypt → PUT to the presigned URL → reference `objectKey` in the MLS message → download-grant → GET → decrypt → render — rides #34/#36 (A3)._
- [x] 34. **Client-side image encryption** with a random content key 🔒 — _A3 (this PR): the composer encrypts each file under a fresh CSPRNG content key (`encryptAttachment`, A1), uploads the ciphertext to the presigned S3 PUT URL, and references it inside the MLS message via a versioned `{text, attachments[]}` envelope (`lib/message-envelope.ts`) carrying the per-attachment objectKey + content key + IV **E2E** (never the server; `attachment_object_key` left unset → server can't link a message to a blob). **Every** message is wrapped (even text-only) so envelope-shaped user text is unambiguous; pre-A3 bare strings still decode as text. `lib/attachments.ts` `uploadAttachment`; `lib/api.ts` grant + S3 PUT/GET wrappers. crypto-reviewer + security-boundary-auditor PASS._
- [x] 35. **Attachment refs** — encrypted blob upload + `attachments` table (RLS, ciphertext refs) 🔒 — _Table DONE (PR #57): migration `0011_attachments.sql` — tenant-scoped, ENABLE+FORCE RLS + WITH CHECK + leading-`tenant_id` index; composite-FK tenant pinning — `(tenant_id, conversation_id)→conversations` (CASCADE; the **server-verified owning conversation** that drives member-only download authz, **never a client message ref** — closes a confused-deputy/IDOR) + `(tenant_id, uploaded_by)→users` (NO ACTION — preserves history like `messages.sender_user_id`); **METADATA + ciphertext-refs ONLY** (`object_key` blob handle — **globally unique** + a CHECK pinning it to the row's `tenant_id` prefix so cross-tenant blob aliasing fails closed at the schema (the blob store is outside RLS) — + `byte_size` + uploader + timestamps — NO content, NO content key, NO plaintext content-type, NO URL). Prunable (`select, insert, delete` to `argus_app` for the #37 cleanup worker; no UPDATE). 12-test live-DB RLS spec (isolation, WITH CHECK, cross-tenant conversation + user composite-FK, object-key tenant-prefix CHECK, byte_size check, global-unique, prunable delete, conversation-delete cascade, NO-ACTION user-delete block, tenant-teardown cascade, fail-closed). `security-boundary-auditor` PASS. Threat model `encrypted-attachments.md`. **Server UPLOAD now BUILT (#33, A2):** `POST /attachments` mints the grant + writes the row (`byteSize` Zod-capped, download membership-404, `uploaded_by` from the verified token, presigned URL never logged) — all the MUST-WIRE items landed. **Stays `[~]`:** the client populates it (encrypt → PUT → ref in the MLS message), A3._
- [x] 36. **Download + decrypt** — recipient renders; member-only authz 🔒 — _A3 (this PR): on receive, `backfill`/`receive` decode the envelope → refs; the `AttachmentImage` component **lazily** requests a download grant → GET ciphertext → `decryptAttachment` (GCM fails closed on a swapped blob) → renders inline (object URL, session-cached); non-image files get a working Download (`saveAttachment`). Refs (incl. content key) persist in the **sealed** message log so attachments survive reload (re-downloaded on demand). Member-only authz + the hard 10 MiB cap are enforced at the server download grant (A2). Web suite (envelope round-trip + E2E attachment-in-envelope messaging test) green._
- [x] 37. **Limits + lifecycle** — size limit (no type limit) + expiry/cleanup 🔒 — _A4 (PR-2): **no content-type limit** (any file ≤10 MiB — the server stores no content-type, stays crypto-blind); `createUploadGrant` stamps `expires_at = now()+7d` (DB clock). Standalone **bash cleanup worker** (`infra/cleanup/`, systemd timer daily) reaps expired blobs + rows: connects as a least-privilege **`argus_cleanup`** role whose RLS policy (migration `0013_attachments_cleanup.sql`) exposes ONLY expired rows across tenants (the tenant-isolation policy is scoped `TO argus_app`); deletes the **B2 object first, then the row** (idempotent, IDs-only logging); secrets via systemd `LoadCredential` ← Key Vault (Managed Identity), libpq `PG*` so no password in `ps`. B2 lifecycle rule (hide@14d / delete-hidden@1d) safety net. 15-test live-DB RLS spec (expired-only + cross-tenant + reap-only + cannot insert/update). Threat model `encrypted-attachments.md`. security-boundary-auditor + infra-reviewer gates._
- [x] 38. **Re-audit** — 42Crunch incl. attachment routes — _Re-audited the full generated spec (all routes documented — attachments, the three `/welcomes`, `/sync`, receipts) → **42Crunch 100/100** (Security 30/30 · Data 70/70), clean. Fixed the one finding: the `413` on `POST /attachments/download-url` carried no body schema; `openapi.ts` now types **any** controller-declared 4xx/5xx with the shared `ErrorResponse` envelope (not just the standard set), so future manual error responses can't regress. Free-trial CLI; `ENABLE_42CRUNCH` stays false in CI._

## Phase 5 — Frontend PWA

> Goal: installable on every platform, no app store.

- [~] 39. **Installable PWA** — manifest + service worker + offline shell; Lighthouse PWA pass — _Vite PWA shell wired (PR #58): `vite-plugin-pwa` (`autoUpdate`) builds a `manifest.webmanifest` + service worker (`sw.js`); standalone display, app icon, EU-safe (no external requests). Installability metadata and the offline shell policy are now tested: root `id`/`scope`/`start_url`, local SVG + 192/512 PNG icons, static precache, `/index.html` navigation fallback, and no runtime cache for auth/API/WS/presigned URLs. **Installability hardened + local Lighthouse PWA pass + platform-ready icon set DONE** (#124/#130/#145; see #44a). **Stays `[~]`:** only the iOS installed-PWA proof (S1, USER) is pending._
- [x] 40. **Web Push** — content-free VAPID notifications; iOS installed-PWA path verified — _VAPID `web-push` fan-out (content-free `{"type":"new_message"}` ping). `push_subscriptions` table (RLS, cascade-on-device-delete, SSRF guard, device-ownership check, 410 self-heal). `PUT`/`DELETE /push/subscription` (throttled, bearer-auth). Custom `injectManifest` SW with push+notificationclick handlers. Client `subscribeToPush`/`unsubscribeFromPush` + Settings "Notifications" toggle. `security-boundary-auditor` PASS; threat model `web-push.md`._
- [~] 41. **Core UX** — conversation list, composer, image, delivery states — _Chat UI rebuilt natively in the Vite PWA to match the **reworked design** (replacing the earlier #58 cut): a carousel **sign-in landing with no registration form** (Zitadel hosts credentials), and a chat with a profile row, new-conversation, search, message thread (direct + group, image/file attachments via local data URIs — never object URLs), composer with previews, delivery-state ticks (sending/sent/delivered/read/**failed**), offline **generated** avatars (no external image requests). **The chat sits behind a real OIDC login gate** (`RequireAuth` + auth context; this PR) — the landing's Passkey/Google/Apple buttons start the Zitadel sign-in, and demo mode (OIDC unconfigured) still runs the seed. **Sending runs a real in-browser MLS encrypt→decrypt round-trip** via `@argus/crypto` (PR #59) — a lock shows once a message is through it; a failed round-trip marks the bubble failed, never sent (no false delivery signal). **Stays `[~]`:** seed-driven until the live fetch→decrypt→render loop + a remote recipient land (MLS Welcome delivery, fingerprint #20)._
- [x] 42. **Key-loss UX** — backup prompt + recovery built into the UI — _Recovery panel built (PR #60): back up (passphrase → downloads the **identity-only** recovery artifact) + **data-loss-safe restore** (verifies the artifact BEFORE any no-clobber clear, so a wrong passphrase/file never wipes the existing device; `DeviceExistsError` gates the clear+re-import). Amber key-loss warning; restore-tab note that recovery brings back the account, **not** past messages (forward secrecy, `key-backup.md` §4). **First-run recovery reminder (local, dismissible) + client-only passphrase-strength meter now DONE** (#131; F3, see #44a). **Server upload + restore-from-server DONE** (PR #163): `storeBackup` (PUT, 204) + `fetchBackup` (GET, 404→null) in `api.ts` via the typed client — Bearer attached, no token/blob logged; `RecoveryPanel` uploads best-effort after local download and reports the outcome honestly (`saved`/`failed`/`local-only`); "Restore on this device" gains a Fetch-from-server path; MUST-WIRE held: identity-only artifact uploaded, never the at-rest keystore blob (forward secrecy preserved); `crypto-reviewer` + `security-boundary-auditor` PASS._
- [x] 41a. **Live client message loop** — wire the chat to the real server (device provisioning → start 1:1 → join → live send/fetch/receive), replacing the seed/loopback — _Slice 2 DONE: **device provisioning on login**. A passphrase **unlock/create gate** (`DeviceProvider` + `UnlockGate`) unseals the device keystore for the session (demo mode passes through), then **provisions + publishes a one-time KeyPackage pool** to the directory (#19) so peers can claim one. `@argus/crypto`: `mintKeyPackage` (fresh one-time KeyPackage under the stable signature identity), `serializeKeyPackage`/array codec (tagged-JSON wire form). Keystore: a sealed `key-package-pool` store (privates retained for join), `ensurePool(target)`. Idempotent re-publish each login (server dedups). `crypto-reviewer` + `security-boundary-auditor` PASS; threat model `device-provisioning.md`. **Slice 3 DONE: start a 1:1** (this PR). `@argus/crypto` `serializeInvite`/`deserializeInvite` (tagged-JSON wire codec for the MLS Welcome + ratchet tree — blank-node-safe, proven by an `addMember → base64 → join → decrypt` round-trip). Client: `api.ts` `listUsers`/`claimKeyPackage`/`createConversation`/`deliverWelcome` wrappers; a **two-phase, safety-number-gated** `ConversationManager` — `prepare()` claims + derives the #20 number, `confirm()` runs `addMember` + create + deliver ONLY after out-of-band confirmation (the structural MITM defense); a contact-picker → `VerifySecurity` (live mode) → create flow wired to the previously-inert "New Conversation" button (demo mode keeps the seed/loopback path). `crypto-reviewer` PASS; crypto 34 + web 38 tests; threat model `start-conversation.md`. **Slice 4 DONE: join on connect** (this PR). `@argus/crypto` `joinConversationFromPool` (ref-match a Welcome to the ONE retained one-time private it was sealed to — `secrets[].newMember` vs `makeKeyPackageRef`, verified against ts-mls' own join matching) + `deviceSignatureSeed` (ts-mls' 48-byte PKCS8 Ed25519 key → the 32-byte seed device proofs need). Keystore `removePoolMember` (CAS-guarded, race-safe FS prune — built here, wired in Slice 5). Client: `api.ts` `listWelcomes`/`fetchWelcomeMaterial`/`consumeWelcome` (base64url proofs); `DeviceProvider` exposes `deviceId`; a `join.ts` orchestrator runs on connect from `ChatScreen` — **list → fetch → join with the matching retained private → surface**, draining across the bounded **cursorless** list (re-list + `seen` set), with a `workingPool` that never reuses a spent one-time private within a drain (FS). **Consuming the Welcome + pruning the private are DEFERRED to Slice 5** — deleting a Welcome before its group state is durably persisted would lose the conversation on reload (Codex P1), so a joined Welcome is left pending as the reload anchor; only **stranded** (unjoinable) Welcomes are consumed-to-clear so they don't block the page. `crypto-reviewer` + `security-boundary-auditor` PASS; crypto 37 + web 52 tests; threat model `join-conversation.md`. **Slice 5 PR-5A DONE (#70): sealed MLS group-state persistence.** `@argus/crypto` `Conversation.serialize()`/`persistVia(persister)` (snapshot + seal + write run INSIDE the op mutex) + `MlsEngine.deserializeConversation` via `encodeGroupState`/`decodeGroupState` (NOT the tagged codec — `GroupState` holds a `Map`); a sealed, identity+signature-bound `group-state` store (IndexedDB v4) written through `persistVia`, with a monotonic `version` + readwrite-tx **CAS** so a stale cross-tab write is refused (`GroupStateConflict`) — durable state stays monotonic, so no MLS generation/AEAD-nonce reuse on reload (2× Codex P1 resolved: in-mutex ordering, then cross-instance CAS). `crypto-reviewer` PASS. **Slice 5 PR-5B DONE (this PR): live send + fetch.** `api.ts` `sendMessage`/`fetchMessages` (ciphertext + metadata only; `/sync` rides 5C); `lib/messaging.ts` `sendLiveMessage` (**encrypt → persist → POST**) + `backfillConversation` (**fetch → decrypt peer-only → persist**), both under a per-conversation **single-writer lock** (`lib/locks.ts`, Web Locks + in-process fallback — the lock promised in 5A's rollback note); the join flow now **persists → consumes → prunes** (closing the Slice-4 deferral); `DeviceContext` retains the session passphrase (memory only) + rehydrates conversations on unlock; `ChatScreen` routes live send + fetch-on-open through these and drops the demo loopback for live conversations; `Conversation.epoch` getter (fails loud past 2^53) for the send metadata. `crypto-reviewer` + `security-boundary-auditor` PASS; crypto 40 + web 67 tests; threat model `live-messaging.md`. **Slice 5 PR-5C DONE (this PR): live WebSocket push.** `lib/ws.ts` `createMessageSocket` — a reconnecting client that authenticates in the **first app frame** (`{event:'auth',data:{token}}`, never a token in the URL/query), subscribes each live conversation, and surfaces pushed ciphertext envelopes; `lib/messaging.ts` `receiveLiveMessage` decrypts + persists one push under the conversation lock (skips self/undecryptable); on every (re)connect the socket re-auths + re-subscribes, and the per-conversation **catch-up back-fill** runs on each `subscribed` ACK — only after the gateway has joined the socket to the room, so no message slips between the catch-up fetch and the live subscription (reuses 5B's `backfillConversation` from each keyset cursor — no `/sync` needed); dedup across push + fetch is by the server message id; capped-exponent backoff + CSPRNG jitter. `ChatScreen` owns one socket (`addLive` subscribes new conversations); dev Vite `/ws` proxy. `security-boundary-auditor` PASS (token-in-first-frame-not-URL, no secret logging, crypto-blind, message-frames-gated-on-auth). crypto 40 + web 78 tests; threat model `live-messaging.md`. **The live client message loop is COMPLETE** (send + fetch-on-open + live push, end-to-end E2EE over the real API). **Persisted message history DONE (this PR):** a local **sealed message-history log** so conversations survive a reload (the ratchet can't re-derive consumed messages). `@argus/crypto` session-key helpers (`deriveSessionKey` + `sealWithKey`/`openWithKey`, AES-256-GCM, fresh CSPRNG IV per seal, conversationId bound into the AAD, params self-described); keystore `message-log` store (IndexedDB v5) sealed under a **per-unlock session key** (one Argon2id at unlock, in memory only) so per-message persistence is cheap AES-GCM; `DeviceContext` derives + holds the key; `ChatScreen` seeds history on rehydrate + appends on send/receive. `crypto-reviewer` PASS (applied: store Argon2 params with the salt; bind the conversationId in the AAD); crypto 47 + web 85 tests; threat model `message-history.md`. **Follow-ups (separate):** migrate the group-state seal onto the session key (the last per-action Argon2), append-only history segments (the O(n) re-seal), full multi-tab leader election, attachment transport, and the carried server-side KeyPackage revoke (device-provisioning §6 / task #20)._
- [x] 43. **Code-delivery hardening** — CSP + SRI + service-worker pinning; published bundle hash 🔒 — _**Security-header slice DONE** (PR #150): the full hardening header set is now **served by Caddy** at the app origin (`infra/stack/caddy/Caddyfile`) — a strict **CSP** (`script-src 'self'` — no inline scripts, ts-mls is pure JS so no `wasm-eval`; `connect-src` scoped to same-origin + the Zitadel issuer + the B2 presigned-URL bucket subdomain; `img-src 'self' data: blob:`; `object-src`/`base-uri`/`frame-ancestors 'none'`), **`Referrer-Policy: no-referrer`** (tightened from `strict-origin-when-cross-origin` — was the one open header gap), a **`Permissions-Policy`** denying unused sensor/hardware features, + the prior `X-Content-Type-Options`/`X-Frame-Options`. `caddy validate` clean; targets from `frontend-observability.md` (#123). Smoke-test CSP against the live app at arming (browser console). **Frontend-build slice DONE** (this PR): **SRI** — `vite-plugin-sri3` (zero-dep, MIT, Vite-8 peer) injects sha384 `integrity` on every built `<script>`/`<link>`; verified the SW precaches the SRI'd `index.html` (precache revision == file md5) so offline loads stay integrity-checked. **Service-worker pinning** — Caddy serves `sw.js`/`workbox-*.js`/`manifest.webmanifest` + HTML with `Cache-Control: no-cache` (always revalidate — the SW is the one script SRI can't cover) and content-hashed `/assets/*` as `immutable`. **Published bundle hash** — a `writeBundle` Vite plugin emits `bundle-manifest.json` (sha384 per JS/CSS == the SRI values + one deterministic `bundleDigest`), served for independent verification + the future security page (G7). Threat model `code-delivery-integrity.md`; crypto-reviewer + infra-reviewer PASS._
- [x] 44. **A11y + responsive** — WCAG AA pass; mobile/desktop layouts — _Baseline DONE: a11y/responsive Playwright coverage (landmarks, keyboard focus return, hidden-menu tab stops, mobile chat/sidebar nav) + a focus-semantics sweep (accessible names, `aria-expanded`/`aria-current`, modal focus entry) + desktop/mobile QA, plus mobile chat/settings + login-portrait polish (#126/#128/#129/#141/#146; F1, see #44a). **WCAG 2.1 AA audit COMPLETE (this PR):** all axe violations resolved — contrast (`--argus-color-text-muted` 45%→60%, all `text-white/30`–`/40` text instances bumped to `/60`), focus trap in `Modal.tsx`, `aria-label` on all password inputs (`UnlockGate`), `aria-hidden` on decorative icons, `aria-live` on attachment chips, `aria-disabled` on placeholder ToS/Privacy links, `prefers-reduced-motion` guard on carousel auto-play. `text-white/40` on icon-only controls passes WCAG 1.4.11 (3:1 non-text). Signed-off audit doc: `docs/threat-models/a11y-audit.md`. Axe E2E spec: `e2e/wcag-audit.spec.ts` (tag `@a11y`). Deferred (not AA violations): 44×44 target size (WCAG 2.5.5 AAA), NVDA/VoiceOver live AT pass._
- [x] 44a. **Frontend maintainability + PWA/UX hardening pass** — the canonical 14-step `apps/web` upgrade + F1–F6 follow-ups: design tokens, reusable UI primitives, route-owned app shell, settings-section split, pseudonymous-profile boundary, typed API-client boundary, versioned browser persistence, chat-hook decomposition, safe async/error states, PWA caching safety, privacy-safe telemetry boundary, and a11y/Lighthouse/bundle/update-prompt polish — _Full plan + standing rules (Non-Negotiables, Identity Model, Browser-Storage classification, the `/chat`-runnable + `localhost:5173` dev rules, the a11y/PWA-cache/no-secrets-in-logs invariants) live in **`docs/frontend-plan.md`** — kept canonical so the process is resurfaceable; this checkpoint is its roadmap anchor. **Done-when:** all 14 steps + F1–F6 checkboxes `[x]` in `frontend-plan.md` and `pnpm frontend:verify` green — both hold; PRs #87–#146 merged. The verify + PR loop are codified as `pnpm frontend:verify` + `scripts/frontend-pr-gate.sh` (Step 2, #89) — the "do not merge on green CI alone" rule (AGENTS.md) holds. **Steps:** route-ownership inventory (#87/#88), Argus design tokens — purple default + the user-selectable accent list (#91), reusable UI primitives — `Button`/`IconButton`/`Modal`/`Avatar`/`SettingsRow`/`StateBlock` with their a11y contracts (#92) then mechanical adoption (#93), route-owned app shell extending the existing `react-router-dom` boundary with `/chat` + `/auth/callback` kept isolated (#95), settings split into section components — mobile-first list→section, dark-only + 10 font sizes, recovery embedded under Security (#97; app-version + status polish #113/#114), pseudonymous-profile boundary — Zitadel = auth subject, Argus profile = generated id + display name + bounded avatar, no username/password or email-derived identity UI (#98), typed API-client boundary inferring `@argus/contracts` Zod types (#99), versioned browser persistence — namespaced `argus:v1:*` keys + scoped-namespace wipe (#105), chat decomposition into hooks (#107/#109/#112/#118), safe async/empty/error states — `toSafeUiError` + reconnect/offline banner (#119), PWA caching restricted to static assets (#120), privacy-safe telemetry boundary (#122), and hosting-header targets + bundle visibility documented in `docs/threat-models/frontend-observability.md` (#123, Step 14C — CSP/SRI/SW-pinning **delivery** is #43). **F1–F6 polish DONE:** a11y/responsive baseline + focus semantics + QA coverage (#126/#128/#129; F1, also advance #44), local Lighthouse PWA pass + installability hardening + platform-ready icon set (#130/#124/#145; F2, also close #39's Lighthouse residual), key-loss UX polish — first-run recovery reminder + passphrase-strength meter (#131; F3, also advance #42), PWA update prompt under About via `workbox-window` — the one justified new dep (#132; F4), UI-consistency + mobile/settings/chat-interaction polish (#134/#141/#146/#116; F5), and bundle lazy-loading of non-chat routes/settings/recovery — main JS 739→697 KiB (212.9 KiB gzip), chat/auth-callback/crypto kept eager (#143; F6). `crypto-reviewer`/`security-boundary-auditor` not required (pure client refactor under the existing invariants — no crypto or server-boundary change)._
- [x] 44b. **Generated pseudonymous identity — DiceBear avatars + `<Adjective> <Animal>` handles** — upgrade the current placeholder generated avatar/name with two pieces: (1) **DiceBear** avatars rendered **LOCALLY** via the `@dicebear/*` npm packages (deterministic SVG seeded from the Argus id) — **never `api.dicebear.com` or any external request** (holds the EU-safe / no-external-image-request non-negotiable, `frontend-plan.md` §B Non-Negotiables); (2) random display handles of the form **`<Adjective> <Animal>`** drawn from a **200-adjective × 200-animal** word list, generated randomly with **per-word dedup** — a used adjective or animal is skipped so neither side ever repeats (unique handles, no duplicates on either side). Pseudonymous + client-side, no PII; extends the #44a profile boundary (Zitadel = auth subject, Argus identity = generated). **Must-hold:** no external avatar/font/CDN fetch (DiceBear bundled, not API). **Pin at impl:** dedup SCOPE — local seed/demo only vs persisted-per-device vs server-global directory uniqueness; per-word dedup caps at ≤200 distinct handles, so a large user base would need a bigger word list or per-pair dedup (200×200 ≈ 40k). Frontend track. — _PR #169: `@dicebear/core` + `@dicebear/lorelei` bundled locally; deterministic SVG avatar seeded from the Argus user id (`lib/dicebear.ts`); server-generated `<Adjective> <Animal>` handle displayed in profile + chat header + contact picker. No external requests; E2E updated._

## Phase 6 — Hardening & observability

> Goal: production-grade reliability and visibility (without leaking content).

- [ ] 45. **Default-deny network isolation** — Azure NSG drops all inbound (no open ports; the VM reaches out only via the Cloudflare Tunnel) + Cloudflare as the edge; verified 🔒
- [x] 46. **Rate limiting + abuse protection** (API) 🔒 — _global per-**verified-user** throttle (`@nestjs/throttler`): a 120/min baseline on every HTTP route + tighter `@Throttle` caps on the abuse-prone mutations earlier threat models deferred here — claim 30, publish/revoke/backup-store 12, backup-fetch 20, attachment upload-grant 30, download-grant 60. `UserThrottlerGuard` keys on the **verified** `tenant:sub` (never client input; IP fallback pre-auth), runs after `JwtAuthGuard`, and skips WS + `@Public` in lockstep with the auth guard. `429` now documented on every operation (`STD_ERROR_RESPONSES`). In-memory counters (single-VM); Redis store is the multi-instance upgrade. The realtime **WS gateway carries its own** per-socket `subscribe`-frame cap (dedup + 120/60s) so the `isMember` DB lookup can't be hammered outside the HTTP caps. Unauthenticated-flood protection is the edge's job (Caddy/WAF). Threat model `rate-limiting.md`; `security-boundary-auditor` PASS._
- [~] 47. **Metrics + dashboards** — Prometheus + Grafana + Alertmanager (Docker Compose on the VM); SLOs defined — _Built as gated code: **Slice A** (#133) — a content-blind `/metrics` endpoint on a separate internal port (prom-client; `{method, route-template, status}` labels only — no content/IDs/query; security-boundary-auditor PASS). **Slice B** (#135) — `prometheus` (scrapes `api:9090`) + `grafana` (behind Cloudflare Access) + `alertmanager` Compose services, hardened + no published ports; alert rules + the **SLOs** (availability · 5xx <1%/5% · p95 <1s) + a starter dashboard; Grafana admin password from Key Vault. Threat model `observability.md`. **Stays `[~]`** — deploys with the stack at arming; dashboards/thresholds tune against real traffic._
- [~] 47b. **Centralized logs** — self-hosted **Loki** + a lightweight collector (Grafana **Alloy**) on the VM, queried in the existing Grafana (#47). Structured app logs are **IDs/metadata only** — never message content, keys, tokens, or full `Authorization` headers (invariant #2); scrubbing enforced before ship. Complements #47 (metrics) + #48 (error tracking): metrics say *how often*, logs say *what happened*, error tracking says *which line/release*. Gated; deploys with the stack at arming. 🔒 — _**BUILT** (this PR): `loki` (filesystem, 7d retention, no auth — internal) + `alloy` (collector) added to `compose.prod.yaml`, **no published ports**; a **Loki datasource** provisioned into the #47 Grafana. Alloy tails container json-logs from a **read-only** `/var/lib/docker/containers` mount — **NO Docker socket** (daemon-root-equiv; rejected) — running uid 0 only to read the root-owned logs, bounded by `cap_drop:[ALL]` + read-only rootfs. An Alloy **scrub stage** masks bearer/JWT/presigned-URL shapes (defense-in-depth for #2) on top of the app's IDs-only discipline. Threat model `centralized-logs.md`; `infra-reviewer`. **Stays `[~]`** — deploys with the stack at arming (verify/refresh the loki/alloy image tags then)._
- [x] 48. **Error tracking** — `@sentry/node` SDK with strict PII/content **scrubbing** (invariant #2: no plaintext/keys/tokens/headers/presigned URLs ever leave), **DSN-gated** (disabled when the DSN is unset). Backend: self-hosted **GlitchTip** (Sentry-API-compatible) as a gated Compose service on the VM — **no new sub-processor**, EU data residency, consistent with the self-hosted #47 stack; SaaS (Sentry EU) stays a one-line DSN swap if ever wanted (zero lock-in — same SDK + protocol). 🔒 — _**Slice A** (merged): `@sentry/node` init gated on `SENTRY_DSN_FILE`/`SENTRY_DSN` (no-op when unset), default-deny scrubber + non-invasive interceptor, 10 scrubbing unit tests; security-boundary-auditor PASS. **Slice B** (this PR): GlitchTip gated Compose service — `glitchtip-db` (dedicated Postgres, isolated cluster like `zitadel-db`), `glitchtip` (Django/gunicorn, `run-migrate-and-web.sh`, :8000 internal), `glitchtip-worker` (Celery+beat); `docker-entrypoint.sh` wrapper reads secrets from files (Django has no `_FILE` env support); `glitchtip_db_password` + `glitchtip_secret_key` from Key Vault; `glitchtip_database_url` derived by `deploy.sh`; `sentry_dsn` OPTIONAL (seeded empty on first boot, provisioned post-arming); `SENTRY_DSN_FILE` wired to `api`; Caddy host-splits `glitchtip.4rgus.com` (Cloudflare Access gated, no published ports). Threat model `error-tracking.md` (incl. arming checklist). `infra-reviewer` PASS._
- [~] 49. **Backups + restore drill** — Postgres PITR + Blob; a *tested* restore — _**nightly logical backup BUILT** (VM deploy track): `infra/backup/` — a hardened systemd worker streams `pg_dump --format=custom | age -r <pubkey> | aws s3 cp -` to a **private EU B2 bucket** (client-side encrypted, so B2 holds ciphertext only; the age private key stays in Key Vault), with day-granular retention pruning. Least-privilege `argus_backup` role (migration 0015 — read-only, BYPASSRLS so FORCE-RLS tables dump in full, NOLOGIN until provisioned). Threat model `db-backup.md`; security-boundary-auditor + infra-reviewer PASS. **`OnFailure=` alerting** `infra/notify/` — `argus-notify-failure@.service` template + `notify-failure.sh` posts a `fatal` Sentry event to GlitchTip on worker failure; graceful no-op when `sentry_dsn` is unarmed; cleanup worker wired too. (The template was authored here but **bundled/installed nowhere until the BKP-1 remediation** wired it into `deploy.sh` — see `docs/reviews/06-infra-deploy.md`.) **Remaining for `[x]`:** run a real **restore drill** on the VM (runbook in `infra/backup/README.md`) — requires the live VM. (The AWS-CLI-v2-vs-`MemoryDenyWriteExecute` risk is resolved: the BKP-1 remediation dropped MDWE from the two CLI workers per the README fallback. The workers reach Postgres in-container via `docker compose exec` — no published port — and the deploy connectivity-probes `argus_backup` that same way; see `docs/threat-models/db-backup.md` §7.) **PITR** (continuous WAL archiving) is the enterprise upgrade beyond the nightly logical dump._
- [ ] 50. **Resilience** — full security suite green, DR runbook, load test to target concurrency 🔒

---

## Phase 7 — GA / go-to-market (the last mile to selling)

> Not in the 50 — the commercialization layer once the beta is solid.

- [x] G1. **Self-serve tenant onboarding** — org create → admin → invite users — _Backend (PR #177) + web frontend (PR #179): OnboardingGate, CreateWorkspace, JoinWorkspace, `/invite#token` route, admin Team settings (member list, role toggle, revoke, invite management)._
- [x] G2. **Per-tenant SSO** — customers federate their own Entra/Okta/Google (OIDC/SAML) — _PR #182: per-tenant OIDC config UI + `tenant_sso_configs` table (FORCE RLS, one row per tenant); API lazily provisions a Zitadel org + OIDC IdP on first save; `clientSecret` written to Zitadel and never stored; SSRF guard on `issuerUrl` (HTTPS-only, RFC-1918 + loopback + `.local`/`.internal` blocked); rollback on partial provisioning failure. 42Crunch 100/100._
- [x] G3. **Admin panel** — metadata only (users, devices, revoke, audit); never content 🔒 — _Backend (PR #181): `GET /admin/devices`, `DELETE /admin/devices/:deviceId`, `GET /admin/audit` — keyset-paginated, cursor-validated, limit clamped [1,100]; `AdminGuard` (live DB role check) + RLS double-enforcement; 42Crunch 100/100. Frontend (`AdminPanel.tsx`): Devices tab (list + inline confirm revoke) + Audit Log tab (load-more pagination, event-type badges). G1 tenant lifecycle audit events also shipped (PR #180)._
- [~] G4. **🔒 Independent cryptography review** of the MLS integration *(external, paid — deferred; not blocking GA for now)*
- [~] G5. **🔒 Third-party pen test** + remediation *(external, paid — deferred; not blocking GA for now)*
- [x] G6. **GDPR pack** — DPA, processing records, residency doc, deletion/export (metadata) — _PR #184: `GET /me/export` (Art. 20 portability — metadata-only JSON snapshot, rate-limited 2/hr) + `DELETE /me` (Art. 17 erasure — `X-Confirm-Delete` header required, rate-limited 3/day). Deletion cascade: welcomes → pseudonymize `messages.sender_user_id NULL` (preserves offline recipients' ciphertext access) → NULL `tenant_invites.accepted_by` → explicit attachment rows → `users` cascade → `user_tenant_index` → attachment blobs best-effort. Migration `0020_gdpr_account_deletion.sql`. Article 30 processing records + threat model. 42Crunch 100/100._
- [x] G7. **Security page** — protocol, bundle hashes, sub-processors — _PR #185: public `/transparency` route (no auth) — security model (crypto-blind server, MLS RFC 9420, Argon2id key backup), live bundle-integrity display fetching `/bundle-manifest.json`, sub-processors table (Azure germanywestcentral, Backblaze B2 eu-central-003, Cloudflare ingress-only), GDPR rights note. Linked from footer + AboutSettings. E2E test added._
- [x] G8. **Billing/plan gating** — _PR #186: Free/Pro/Enterprise plan tiers; `member_limit` + `sso_enabled` enforced at create-invite + accept-invite + SSO config; `PaymentRequiredException` (HTTP 402); Stripe Checkout + Portal + full 8-status webhook fanout; operator API (`PATCH /operator/tenants/:id/plan`) with timing-safe key auth + SSO teardown on downgrade; plan badge + upgrade CTAs in TeamSettings + AdminPanel; Stripe added to sub-processors; Caddy proxies `/webhooks/*` → api. 42Crunch 100/100._

## Beyond GA — backlog (the deferred hard stuff)

- [x] B1. **Group chat** (MLS groups) — cheap-ish because MLS was chosen up front — _Landed: composite `userId:deviceUuid` MLS identity, group `ConversationManager.confirmCreate()` (fan-out add-commit to all members + self-devices), `GroupConversationManager` UI (group name, multi-select contact picker), group WS routing by conversation room, group message-history persistence. Threat models `group-chat.md`. B1 branch merged._
- [x] B2. **Multi-device sync** — encrypt-to-all-devices + history sync (the nastiest E2EE problem) — _Landed (PR #191): **composite `userId:deviceUuid` identity** (eliminates MLS leaf collision; `formatDeviceIdentity`/`parseDeviceIdentity`), **`device_enrollments` table** (RLS, FKs, 15-min TTL), **enrollment endpoints** (`POST /devices/me/enrollment`, `GET /devices/enrollments`, approve/reject, `GET /devices/me/conversations` fan-out diff), **`enroll` proof domain** (`argus-enroll:v1` — cross-domain non-reuse tested), **enrollment fan-out driver** (`lib/enroll.ts` — sequential add-commit per conversation, 409-rebase), **enrollment UI** (`LinkDevicePanel` D2 side, `ApproveDevicePanel` D1 side), **self-add on conversation create** (`confirm()`/`confirmCreate()` claim own-user's other-device packages), **atomic `POST /devices/me/migrate`** (delete + re-insert as non-provisional in one tx under `FOR UPDATE` — closes the race window that separate withdraw + provision leaves open), **legacy migration path** (pre-B2 bare-userId keystores auto-upgraded on unlock/restore). History: new device decrypts from its add-epoch forward only (forward secrecy; UX copy "History stays on your other device"). `crypto-reviewer` + `security-boundary-auditor` PASS; 42Crunch 100/100. Threat model `multi-device-enrollment.md`._
- [ ] B3. **Per-tenant compliance mode** — opt-in escrow/journaling for regulated buyers
- [ ] B4. **Multi-region / zone-redundant VM deploy**; Azure sovereign-operator option
- [ ] B5. **SOC 2 / ISO 27001 / NIS2** path
