# Copy to .env.local (gitignored) once Zitadel is deployed. Public client (no secret in the SPA).
VITE_OIDC_ISSUER=https://your-zitadel-instance
VITE_OIDC_CLIENT_ID=your-spa-client-id
VITE_OIDC_REDIRECT_URI=http://localhost:5173/auth/callback
